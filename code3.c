#include<stdio.h>
int main()
{
    int area,perimeter,length=10,breadth=5;
    area=length*breadth;
    perimeter=2*(length+breadth);
   printf("areaa=%d\n",area);
   ;
   printf("perimeter=%d",perimeter);
   
}